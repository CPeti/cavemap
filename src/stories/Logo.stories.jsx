import React from 'react';
import Logo from '../components/Logo';

export default {
  title: 'Components/Logo', // How it will appear in the Storybook sidebar
  component: Logo,
};

export const Default = () => <Logo />;
